# PDF parsing — fast local parser (replaces LlamaParse cloud API)
pymupdf4llm==0.0.17

# Web framework
fastapi==0.115.0
uvicorn==0.32.0
python-multipart==0.0.12

# LLM / AI
langchain==0.3.7
langchain-openai==0.2.9
langchain-community==0.3.7
openai>=1.54.4

# Vector store
supabase==2.28.0
vecs==0.4.3

# Embeddings — Jina API (no torch/sentence-transformers needed)
# sentence-transformers and torch are kept as optional fallback for local provider
requests==2.32.3

# Utilities
pydantic>=2.11.7
pydantic-settings>=2.6.1
tiktoken==0.8.0
python-dotenv==1.0.1
tenacity==9.0.0

# Reranker — keep for query pipeline
sentence-transformers==3.3.1
torch==2.4.1
